module.exports = {
  // 缩进宽度
  tabWidth: 2,
  // 使用单引号而非双引号（JSX 除外）
  singleQuote: true,
  // 多行的 HTML 元素中，前标签的 '>' 是否与最后一个属性在同一行
  bracketSameLine: false,
  // 行末使用 '\n'
  endOfLine: "lf",
  // 语句尾部添加分号
  semi: true,
  // 当对象的属性名有一个需要加引号时，将所有属性名都加上引号
  quoteProps: "consistent",
};
